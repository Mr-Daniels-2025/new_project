var arrayString = ["string 1", "string 2", "sting 3", "string 4"];

var numbers = [1,2,3,4];

var boleans = [age = false, height = true, weight = false, eyes = true];

arrayString.pop();
console.log(arrayString);
numbers.push(1);
console.log(numbers);
boleans.slice(2,3);
console.log(boleans);

//index positions represent the physical numerical position inside in an array.
console.log(boleans[0]);
